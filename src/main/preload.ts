// Nothing exposed to the renderer yet — Phase 1 has no main<->renderer
// IPC surface. This file exists so electron-vite has a preload entry
// point ready for when dragging/context-menu IPC is added (Phase 6/7).
export {}
